/// GLOBAL APP CONFIG
///
/// Paste your Groq API key below. Get one free at https://console.groq.com/keys
/// Do NOT commit a real key to a public repo — for production, move this to
/// a secure secret store (Firebase Remote Config, backend proxy, or
/// --dart-define build flags) instead of hardcoding it in source.
class AppConfig {
  static const String groqApiKey = 'PASTE_YOUR_GROQ_API_KEY_HERE';

  static const String appName = 'GemNet';
}
